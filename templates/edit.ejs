<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8" />
    <link rel="stylesheet" href="/style.css">
	<title>Poem Editing</title>
    <script>
        function confirmSubmission(event) {
            if (!confirm("Are you sure you are done editing this poem?")) {
                event.preventDefault();
            }
        }
    </script>
</head>

<body>
	<h1>Edit Poem</h1>
    <form method="POST" action="/edit/<%= id %>" onsubmit=confirmSubmission(event)>
        <fieldset>
            <label><strong>Author: </strong><input type="text" name="author" value= "<%= author %>" required></label>
            <label><strong>Title: </strong><input type="text" name="title" value="<%= title %>" required></label>
            <br>
            <br>
            <textarea name="poem" rows=20 cols=100 required><%= poem %></textarea>
        </fieldset>
        <br>
        <button type="reset">Reset</button>
        <button type="submit">Republish Poem</button>
    </form>
    <hr>
    <a href="/">HOME</a>

</body>

</html>
